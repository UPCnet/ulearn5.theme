<?xml version="1.0" encoding="utf-8"?>
<rules xmlns="http://namespaces.plone.org/diazo"
       xmlns:css="http://namespaces.plone.org/diazo/css"
       xmlns:xhtml="http://www.w3.org/1999/xhtml"
       xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
       xmlns:xi="http://www.w3.org/2001/XInclude">

  <!-- Include the backend theme (Barceloneta) loaded on edit views -->
  <xi:include href="++theme++ulearn5-theme/backend.xml" />

  <!-- Default template loaded -->
  <theme href="community.html" />
  <theme href="homepage.html" css:if-content="body.section-front-page"/>
  <notheme css:if-not-content="#visual-portal-wrapper" />

  <rules css:if-content="#portal-top">
    <!-- Attributes -->
    <copy attributes="*" css:theme="html" css:content="html" />
    <!-- Base tag -->
    <before css:theme="title" css:content="base" />
    <!-- Title -->
    <replace css:theme="title" css:content="title" />
    <!-- Don't use Plone icons, use the theme's -->
    <drop css:content="head link[rel='apple-touch-icon']" />
    <drop css:content="head link[rel='shortcut icon']" />
    <!-- drop the theme stylesheets -->
    <drop theme="/html/head/link[rel='stylesheet']" />
    <!-- Pull in Plone Meta -->
    <after css:theme-children="head" css:content="head meta" />
    <!-- CSS -->
    <after css:theme-children="head" css:content="head link" />
    <!-- ... before your own scripts -->
    <before theme="/html/head/script[1]">
        <xsl:apply-templates select="/html/head/script" /> <!-- include the Plone scripts -->
        <script>
            require = undefined
            define = undefined
        </script> <!-- and then unset require and define -->
    </before>
  </rules>

  <!-- <rules css:if-content="body.section-agenda.subsection-agenda">
    <before css:theme-children="body">
      <div id="widget-config" data-config-url="{$portal_url}">
      </div>
    </before>
  </rules> -->

  <!-- Copy over the id/class attributes on the body tag. This is important for per-section styling -->
  <copy attributes="*" css:content="body" css:theme="body" />

  <drop css:content="#portal-logo" />
  <drop css:content="#portal-searchbox" />
  <drop css:content="#portal-languageselector" />
  <drop css:content="#mainnavigation" />
  <!-- TODO: This hides all the portlets! Test footer footer... -->
  <!-- <drop css:content=".portletWrapper" /> -->

  <!-- Barra Menu Eines -->
  <!-- <replace css:theme=".upc-eines" css:content=".upc-eines" /> -->
  <!-- Barra Menu Venta-->
  <!-- <replace css:theme=".upc-menu" css:content=".upc-menu" /> -->
  <!-- Barra Menu Navegació -->
  <!-- <replace css:theme=".footer-navigation" css:content=".footer-navigation" /> -->
  <!-- Barra Menu Navegació -->
  <!-- <replace css:theme=".footer-socials" css:content=".footer-socials" /> -->

  <!-- HOME -->
  <!-- <replace css:theme=".content-highlighted" css:content=".content-highlighted" />
  <replace css:theme=".content-banners" css:content=".content-banners" />
  <replace css:theme=".content-hotnews" css:content=".content-hotnews" />
  <replace css:theme=".content-well" css:content=".content-well" />
  <replace css:theme=".content-agenda" css:content=".content-agenda" />
  <replace css:theme=".content-recerca" css:content=".content-recerca" />
  <replace css:theme=".content-xifres" css:content=".content-xifres" /> -->

  <!-- Breadcrumb -->
  <replace css:theme-children=".breadcrumbs ol" css:content-children=".plone-breadcrumb ol" />
  <!-- <drop css:theme="#breadcrumbs" if-path="ca/"/>
  <drop css:theme="#breadcrumbs" if-path="es/"/>
  <drop css:theme="#breadcrumbs" if-path="en/"/> -->

  <!-- full-width breadcrumb -->
  <replace css:content="#viewlet-above-content" css:theme="#above-content" />

  <drop css:content=".documentFirstHeading" if-path="front-page/"/>

  <!-- Left column -->
  <rules css:if-content="#portal-column-one">
    <replace css:theme="#column1-container">
      <section class="col-lg-3 col-md-4 col-xs-12 side-bar">
        <xsl:copy-of css:select="#portal-column-one > *" />
      </section>
    </replace>
  </rules>

  <!-- Right column -->
  <rules css:if-content="#portal-column-two">
    <replace css:theme="#column2-container">
      <section class="col-lg-3 col-md-4 col-xs-12 side-bar">
        <xsl:copy-of css:select="#portal-column-two > *" />
      </section>
    </replace>
  </rules>

  <!-- Central column -->
  <replace css:theme="#content-container" method="raw">

    <xsl:variable name="central">
      <xsl:if test="//aside[@id='portal-column-one'] and //aside[@id='portal-column-two']">col-lg-6 col-md-4 col-xs-12</xsl:if>
      <xsl:if test="//aside[@id='portal-column-two'] and not(//aside[@id='portal-column-one'])">col-xs-12 col-sm-9</xsl:if>
      <xsl:if test="//aside[@id='portal-column-one'] and not(//aside[@id='portal-column-two'])">col-lg-9 col-md-8 col-xs-12</xsl:if>
      <xsl:if test="not(//aside[@id='portal-column-one']) and not(//aside[@id='portal-column-two'])">col-lg-12 col-md-12 col-sm-12 col-xs-12</xsl:if>
    </xsl:variable>

    <section id="main-content" class="{$central} main-content">
      <xsl:apply-templates css:select="#content" />
      <footer class="row">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
          <xsl:copy-of css:select="#viewlet-below-content" />
        </div>
      </footer>
    </section>
  </replace>

  <!-- Alert message -->
  <replace css:theme-children="#global_statusmessage" css:content-children="#global_statusmessage" />

  <!-- Content header -->
  <replace css:theme="#portal-top" css:content-children="#portal-top" />

  <!-- Footer -->
  <replace css:theme-children="#portal-footer" css:content-children="#portal-footer-wrapper" />

  <replace css:theme-children="#portal-anontools" css:content-children="#portal-anontools" />

  <drop css:content="#portal-anontools" />
</rules>
